import React from "react"


// Empty for now, goal was to use that part to choose which option to generate
// the letters (are the letters sampled uniformely, or based on a specific distribution)
class LetterGenerationLogic extends React.Component{

    render(){
        return(
            <div id="letterGenerationLogic" style={{justifyContent : "center"}}>
            :-) :-)
            </div>
        )
    }
}

export default LetterGenerationLogic
